package io.oopsie.sdk.basic.model;

/**
 * I guess we could share the DataType enum from the
 * api project instead of this one.
 */
public enum AttributeType {
    OID,
    BINARY,
    BOOLEAN,
    DATE,
    DECIMAL,
    INTEGER,
    TIMESTAMP,
    PLAIN_TEXT;
}
